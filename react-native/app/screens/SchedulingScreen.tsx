import type Node from 'react';
import * as React from 'react';
import {Text, View} from 'react-native';

type SchedulingScreenProps = {

}

export default function SchedulingScreen(props: SchedulingScreenProps): Node {
    return (
        <View>

        </View>
    );
}
